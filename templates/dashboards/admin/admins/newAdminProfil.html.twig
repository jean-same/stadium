{% extends 'baseDashboardAdmin.html.twig' %}

{% block title %}Ajout profil
{% endblock %}

{% block body %}
	<div id="main">

		<header class="mb-3">
			<a href="#" class="burger-btn d-block d-xl-none">
				<i class="fa fa-bars fs-3" aria-hidden="true"></i>
			</a>
		</header>
		<div class="row col-6">
			{{ form_start(formNewAdminProfil) }}

			{{ form_row(formNewAdminProfil.firstName, { 'attr' : { 'placeholder' : "Renseignez votre prénom" } }) }}
			{{ form_row(formNewAdminProfil.lastName, { 'attr' : { 'placeholder' : "Renseignez votre nom" } }) }}
			{{ form_row(formNewAdminProfil.picture) }}
			<button type="submit" class="btn principal-color w-100">
				Ajouter
			</button>
			{{ form_end(formNewAdminProfil) }}
		</div>
	</div>

{% endblock %}
